{
  "author": "AnatolyI <ai@rsl.ru>",
  "name": "poppler-simple",
  "description": "A simple interface to poppler pdf library",
  "keywords": ["pdf", "poppler"],
  "version": "0.0.6",
  "repository": {
    "type": "git",
    "url": "git://github.com/blackbeam/node-poppler.git"
  },
  "scripts": {
    "install": "(node-gyp rebuild) || (exit 0)",
    "preinstall": "(node-gyp configure) || (exit 0)",
    "test": "./node_modules/mocha/bin/mocha -gc",
    "clean": "((node-gyp clean) && (rm -rf node_modules)) || (exit 0)"
  },
  "main": "./lib/poppler.js",
  "engines": {
    "node": ">=0.10.0 <0.12.0"
  },
  "dependencies": {},
  "devDependencies": {
    "mocha": "*"
  },
  "optionalDependencies": {},
  "engines": {
    "node": "*"
  }
}
